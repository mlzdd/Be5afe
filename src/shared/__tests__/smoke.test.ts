// Smoke test — proves the test runner is wired correctly.
// This test should always pass. If it fails, the tooling setup is broken.
describe('chassis', () => {
  it('test runner is working', () => {
    expect(true).toBe(true);
  });
});
